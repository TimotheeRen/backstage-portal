apiVersion: v1
kind: Service
metadata:
  name: backstage-frontend
  namespace: default
  labels:
    service: backstage-frontend
spec:
  selector:
    app: backstage-frontend
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 80
  type: ClusterIP
