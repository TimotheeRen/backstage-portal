apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: default
  labels:
    service: api
spec:
  selector:
    app: api
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001
    - name: metrics
      protocol: TCP
      port: 9000
      targetPort: 9000
  type: ClusterIP
