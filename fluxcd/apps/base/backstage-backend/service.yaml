apiVersion: v1
kind: Service
metadata:
  name: backstage-backend
  namespace: default
spec:
  selector:
    app: backstage-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 7007
  type: ClusterIP
