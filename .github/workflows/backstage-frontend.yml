name: Backstage Frontend CI/CD

on:
  push:
    paths:
      - "backstage/**" # To sync with additional extension config
  workflow_dispatch:

jobs:
  ci:
    uses: timotheeren/backstage-portal/.github/workflows/ci.yml@main
    with:
      image: "backstage-frontend"
      path: "Dockerfile-frontend"
    secrets:
      dockerhub-username: ${{ vars.DOCKERHUB_USERNAME }}
      dockerhub-token: ${{ secrets.DOCKERHUB_TOKEN }}

  cd:
    needs: ci
    uses: timotheeren/backstage-portal/.github/workflows/cd.yml@main
    with:
      image: "timotheeren/backstage-frontend"
      tag: "sha-${{ github.sha }}"
      path: "backstage-frontend"
    secrets:
      token: ${{ secrets.PAT }}
